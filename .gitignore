# dependencies
node_modules/

# DSi BIOS, firmware and NAND images (copyrighted Nintendo system files, never commit)
bin/
*.nand
dsinand*.bin
re/

# emulator source and build artifacts
melonDS/
out/

# raw extracted firmware assets and decode intermediates (large; regenerated by the pipeline)
assets/

# generated verification output (local working notes, not published)
compare/

# local tool / editor state
.claude/
.DS_Store
*.log

# root symlink to webapp/node_modules
/node_modules

# scratch / working dir
.tmp/
